const expect = require('chai').expect
const { proccessSalvar } = require('./botCommands')

describe('TESTE DAS FUNÇÕES DO ARQUIVO BotCommandes.js', () => {
  it('Função processSalvar', () => {
    const salvarMsg = '/salvar 12 doces'
    
  })
})